Temporary same-repository PR trigger for the non-destructive #618 rebase
rehearsal.

Retry 6: the handoff base now targets the Ruff-clean r2 rehearsal branch and
uses the corrected one-line `write_conv_rows` resolution. This head update
forces GitHub to rebuild the PR merge context against base `b38712d`.

This branch contains no implementation change.
